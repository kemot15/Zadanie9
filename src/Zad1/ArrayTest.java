package Zad1;

import java.util.Arrays;
import java.util.Random;

public class ArrayTest {
    public static void main(String[] args) {
        int[] tab = Array.generateArray();
        //System.out.println(Arrays.toString(tab));
        Array.reverseArray(tab);
    }
}
